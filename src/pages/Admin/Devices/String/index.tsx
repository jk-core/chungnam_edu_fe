import { StringBoard } from './components/StringBoard';

/**
 * 스트링 관리 (SFR-016-01, SFR-017-06).
 * 목록에서 조회하고, 등록·수정은 설비 한 대의 스트링을 한꺼번에 다룬다.
 */
function StringDepth() {
  return <StringBoard />;
}

export default StringDepth;
