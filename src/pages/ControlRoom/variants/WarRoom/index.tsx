import { WarRoomBoard } from './components/WarRoomBoard';

/**
 * 통합관제 상황판 · 시안 B — 타임라인 워룸 (SFR-004).
 *
 * 다른 시안은 모두 「지금」 을 답한다. 지금 9MW 라는 값만으로는 그것이 잘 도는 것인지 아침보다
 * 처진 것인지 알 수 없고, 미조치 경보 마흔 건이 하루 내내 흩어진 것인지 한 시각에 몰린 것인지도
 * 알 수 없다. 이 시안은 하루를 가로로 눕혀 그 물음에 답한다.
 *
 * 세 켜가 같은 06–20시 축을 나눠 쓴다. 위 곡선이 「도 전체가 언제 처졌나」, 가운데 히트맵이
 * 「그게 어느 시·군의 일이었나」, 아래 줄이 「그래서 무엇을 손봐야 하나」 를 이어서 답한다.
 * 히트맵에서 시·군 이름을 누르면 아래 줄이 그 지역으로 좁혀져, 세 켜가 한 흐름으로 읽힌다.
 *
 * 바탕을 어둡게 두는 것은 멋이 아니라 신호 대 잡음의 문제다. 관제실은 어둡고 화면은 크다 —
 * 흰 바탕이 화면의 9할을 차지하면 정작 켜져야 할 경고색이 묻힌다. 어두운 바탕에서는 색이 곧 신호다.
 */
function WarRoomPage() {
  return <WarRoomBoard />;
}

export default WarRoomPage;
